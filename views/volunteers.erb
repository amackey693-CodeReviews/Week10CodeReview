<h1> Volunteer </h1>

<h3> Checkout Our Current Volunteers! </h3>

<h4>Would you like to sign up as a volunteer? Fill out the form below!</h4>
<hr>
<form action="/volunteers" method="post">
  <div class="form-group">
    <input id="first_name" name="first_name" class="form-control" type="text" placeholder = "Enter First Name">
    <br>
    <input id="last_name" name="last_name" class="form-control" type="text" placeholder = "Enter Last Name">
    <br>
    <!-- STRETCH GOALS -->
    <!-- <input id="phone_number" name="phone_number" class="form-control" type="tel" placeholder = "Phone Number">
    <br>
    <input id="email" name="email" class="form-control" type="text" placeholder = "Email">
    <br> 
    <input id="user_name" name="user_name" class="form-control" type="text" placeholder = "Username">
    <br>
    <input id="password" name="password" class="form-control" type="password" placeholder = "Password"> -->
  </div>
  <button type="submit" class="btn btn-dark">Submit</button>
</form>
<br>

<% if @volunteers.any? %>
  <ul>
  <% @volunteers.each do |volunteer| %>
    <li> <%= volunteer.name%> </li>
  <% end %>
  </ul>
  <% else %>
<p> Looks like we don't currently have any volunteers! Fill out the box above to sign up!</p>
<% end  %>

<button type="button" class="btn btn-dark"><a href="/home">Home</a></button>